#!/usr/bin/env python2
import argparse
from hotcidr.setupall import main

parser = argparse.ArgumentParser(description='Setup the necessary git repos for the project')

parser.add_argument('config-file', help='The location of the config yaml file containing the information necessary for setup.')

args = vars(parser.parse_args())

main(args['config-file'])
